import light from './Styles/StyleLight'
import dark from './Styles/StyleDark'
import sees from './Styles/StyleSEES'
import phantom from './Styles/StylePhantom'
import truth from './Styles/StyleTruth'

 const themes = {
 light: {
    container: light.container,
    title: light.title,
    buttonsContainer: light.buttonsContainer,
    button: light.button,
    activeButton: light.activeButton,
    buttonText: light.buttonText,
    startButton: light.startButton,
    startButtonText: light.startButtonText,
    cards: light.cards,
    footer: light.footer
  },
  dark: {
    container: dark.container,
    title: dark.title,
    buttonsContainer: dark.buttonsContainer,
    button: dark.button,
    activeButton: dark.activeButton,
    buttonText: dark.buttonText,
    startButton: dark.startButton,
    startButtonText: dark.startButtonText,
    cards: dark.cards,
    footer: dark.footer
  },
   sees: {
    container: sees.container,
    title: sees.title,
    buttonsContainer: sees.buttonsContainer,
    button: sees.button,
    activeButton: sees.activeButton,
    buttonText: sees.buttonText,
    startButton: sees.startButton,
    startButtonText: sees.startButtonText,
    cards: sees.cards,
    footer: sees.footer
  },
  truth: {
    container: truth.container,
    title: truth.title,
    buttonsContainer: truth.buttonsContainer,
    button: truth.button,
    activeButton: truth.activeButton,
    buttonText: truth.buttonText,
    startButton: truth.startButton,
    startButtonText: truth.startButtonText,
    cards: truth.cards,
    footer: truth.footer
  },
  phantom: {
    container: phantom.container,
    title: phantom.title,
    buttonsContainer: phantom.buttonsContainer,
    button: phantom.button,
    activeButton: phantom.activeButton,
    buttonText: phantom.buttonText,
    startButton: phantom.startButton,
    startButtonText: phantom.startButtonText,
    cards: phantom.cards,
    footer: phantom.footer
  },
}

export default themes